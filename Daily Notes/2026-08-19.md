VS Code sucks ass. Switching to JetBrains Ride IDE.
Found out there's a process consuming a lot of RAM `vmmem`, which turns out to be what manages the virtual memory used by the WSL's virtual machines.
It's warned that WSL has memory leaks, whatever that means.
Source: https://www.reddit.com/r/docker/comments/15p5srd/vmmem/
So far, I've encountered many problems trying to run the Clean Architecture solution from Milan Jovanović:
- On VS Code I run the command on the readme to create the containers with with the docker compose yaml and it did without problems.
- But when trying to run the API, I found out that there was a problem when I renamed the solution, and this took me into a rabbit hole to find out why I was having so many problems with solutions on VS Code. I ended up switching to Ride.
- I also found out there was a problem with one of the packages because it had a vulnerability. I think it was one of the packages dependencies, or as they're called in Ride "Implicitly installed packages"
- Once in Ride, I restored and updated all the packages and I had a successful build 🎉
I'll try to build the containers and run the application again.
I'd also like to at least try to get through one of the Docker tutorials I have saved.

![[Pasted image 20260819175317.png]]
????

Another problem: `Unhandled exception. Npgsql.NpgsqlException (0x80004005): No such host is known.`
It's not connecting to the db container.
I tried to access localhost through the same tcp port as the container and that was fine, but then trying to access 'postgres' - the container's name -  I wasn't able to.
(I had to enable telnet to do this: https://help.wnpower.com/hc/es/articles/360036042471-C%C3%B3mo-activar-el-comando-Telnet-en-Windows-10)
**Solution:** the connection string was pointing to postgres as the host when it should've been localhost.

Installed postman to make request but swagger is also available at `localhost:{port}/swagger`
All tests have run successfully :)

I think having a template with ready-made code will help me start implementing things while applying the knowledge and experience I have so far. It will as well help solve problems as I encounter them and understand the why of things in Clean Architecture and DDD.